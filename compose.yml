services:
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_DB: trongate
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: secret
    ports:
      - "5555:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: